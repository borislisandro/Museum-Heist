/**
 * Package for all the logger related classes.
 * It contains the ConcurrentLogger itself and all internal state classes.
 * These classes are NOT thread safe and require locks to be manipulated.
 */
package logger;